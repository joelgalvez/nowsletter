<%= form_with url: url, method: :get, class: "flex items-center gap-2", data: { controller: "search", turbo_frame: frame_id, turbo_action: "advance" } do |f| %>
  <div class="relative rounded-md shadow-sm">
    <%= f.text_field :query,
      value: query,
      placeholder: placeholder,
      class: "block w-full pr-10 sm:text-sm border-gray-300 rounded-md focus:ring-indigo-500 focus:border-indigo-500",
      data: {
        action: "input->search#submit",
        search_target: "input"
      },
      autocomplete: "off" %>
    <div class="absolute inset-y-0 right-0 pr-3 flex items-center">
      <% if query.present? %>
        <%= link_to url_for(sort: sort, direction: direction), class: "text-gray-400 hover:text-gray-600", data: { turbo_frame: frame_id } do %>
          <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
          </svg>
        <% end %>
      <% else %>
        <svg class="h-5 w-5 text-gray-400 pointer-events-none" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
          <path fill-rule="evenodd" d="M8 4a4 4 0 100 8 4 4 0 000-8zM2 8a6 6 0 1110.89 3.476l4.817 4.817a1 1 0 01-1.414 1.414l-4.816-4.816A6 6 0 012 8z" clip-rule="evenodd" />
        </svg>
      <% end %>
    </div>
  </div>
  <%= f.submit "Search", class: "hidden" %>
<% end %>
